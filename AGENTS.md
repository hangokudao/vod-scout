# VOD Scout 작업 규칙

이 파일은 이 프로젝트 아래의 모든 파일에 적용한다. 상위 세션 지침과 충돌하면 상위 지침이 우선한다.

## 1. 변경 범위

- 사용자 요청과 직접 연결된 파일만 수정한다.
- 사용자의 실제 작업 데이터와 기존 설치 폴더는 명시적 승인 없이 수정·삭제하지 않는다.
- 다른 점검이나 빌드가 동시에 실행 중이면 같은 파일과 설치 산출물을 덮어쓰지 않는다. 최근 변경과 관련 프로세스를 먼저 확인한다.
- OpenAI, Claude 등 유료 API는 사용자가 별도로 승인하기 전에는 제품 실행 경로에 추가하지 않는다.

## 2. 버전 정본

버전을 올릴 때 아래 값을 반드시 같은 버전으로 맞춘다.

- `package.json`
- `src-tauri/Cargo.toml`
- `src-tauri/tauri.conf.json`
- README의 버전 설명과 설치 파일 링크
- 설치 파일명

하나라도 다르면 릴리스는 `HOLD`다.

## 3. 버전업 필수 문서

모든 버전업은 코드 변경과 함께 아래 문서를 갱신해야 한다.

1. `docs/V{버전}-RELEASE.md`: 해당 버전의 목표, 범위, 구현, 테스트, 리뷰, 보안, 알려진 한계, 롤백 방법
2. `CHANGELOG.md`: `Added`, `Changed`, `Fixed`, `Security`, `Known issues` 중 해당 항목
3. `docs/DEBUGGING.md`: 실제로 조사한 오류의 증상, 원인, 수정, 회귀 테스트. 추측만 한 내용은 `HOLD`로 기록
4. `HANDOFF.md`: 현재 완료 상태, 마지막 PASS, 다음 정확한 작업, HOLD 사유
5. `BUILD-MANIFEST.md`: 설치 파일과 핵심 바이너리의 크기·SHA-256, 도구·모델 버전, 패키지 검증 결과

문서가 빠지거나 실제 결과와 다르면 버전업 완료로 보고하지 않는다. 작성 형식과 게이트는 `docs/RELEASE-PROCESS.md`를 따른다.

## 4. PR 형식 리뷰 게이트

- 유효한 Git 저장소와 승인된 원격 저장소가 있으면 릴리스 전에 PR 리뷰를 수행한다.
- Git 저장소가 없으면 `docs/V{버전}-RELEASE.md`의 `PR 형식 자체 리뷰` 절에 변경 파일, 위험, 회귀 가능성, 발견 사항, 수정 여부를 기록한다.
- 리뷰에서는 최소한 입력 검증, 경로·삭제 범위, 체크포인트 호환성, 자식 프로세스 종료, 외부 바이너리 해시, 개인정보·외부 전송, 장시간 자원 사용을 확인한다.
- 리뷰 발견 사항은 `PASS`, `HOLD`, `BLOCKED`로 구분한다. 미확인 사항은 PASS로 처리하지 않는다.

## 5. 버그와 취약점 기록

- 버그 수정은 재현 조건, 원인, 수정 내용, 회귀 테스트를 `docs/DEBUGGING.md`와 `CHANGELOG.md`의 `Fixed`에 함께 기록한다.
- 취약점 수정은 영향 경로, 심각도, 수정 내용, 검증 방법을 릴리스 문서와 `CHANGELOG.md`의 `Security`에 기록한다.
- 의존성 경고는 개수만 보고 취약점이라고 단정하지 않는다. Windows 제품 경로 도달 여부와 실제 사용 기능을 확인한다.
- 설치 파일, 내장 실행 파일, 모델은 고정 SHA-256과 실제 파일 해시가 일치해야 한다.
- 설치본과 빌드 산출물의 해시가 다르면 원인을 확인하기 전까지 패키지 게이트는 `HOLD`다.

## 6. 구현과 검증 순서

1. 요구사항과 완료 조건을 릴리스 문서에 고정한다.
2. 체크포인트와 이전 작업 데이터 호환성을 먼저 설계한다.
3. 최소 변경으로 구현한다.
4. 관련 단위 테스트와 실제 진입점 E2E를 실행한다.
5. PR 형식 리뷰에서 발견한 문제를 수정하고 재검증한다.
6. 설치본을 생성하고 실제 설치본의 버전·리소스·해시·실행 경로를 검증한다.
7. 모든 문서와 `HANDOFF.md`를 실제 결과로 갱신한다.

기본 검증 명령은 다음과 같다.

```powershell
npm.cmd test
npm.cmd run build
cargo test --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/fixture-worker/Cargo.toml
```

실제 미디어, YouTube, 장시간 테스트는 입력·하드웨어·처리 시간·피크 메모리·결과를 릴리스 문서에 기록한다. 실행하지 못한 테스트는 `HOLD`로 남긴다.

## 7. 오픈소스·설치 파일 배포

- 공개 목표는 public 소스 저장소와 Windows 설치 EXE를 함께 제공하는 것이다. 설치 파일이 없는 소스 공개만으로 릴리스 완료를 선언하지 않는다.
- 설치 EXE, 내장 실행 파일, 모델, 실제 미디어는 일반 Git 커밋에 넣지 않는다. 설치 EXE는 보안 게이트 통과 후 GitHub Release 자산으로 배포한다.
- 첫 공개 릴리스 전 `LICENSE`, `SECURITY.md`, `CONTRIBUTING.md`, 체크섬, SBOM, 릴리스 노트, 직접 다운로드 스모크가 모두 필요하다.
- 공개 전 private staging과 공개 전환은 `docs/GIT-PRIVATE-REPOSITORY-PLAN.md`, 배포 자산과 설정은 `docs/OPEN-SOURCE-RELEASE-PLAN.md`를 따른다.
- 첫 공개가 끝나면 public 저장소를 일반 개발과 기여의 정본으로 사용한다.

## 8. 사용자 문구와 UI 표현

- 사용자에게 보이는 문서와 UI에서는 영어 개발 용어를 직역하지 말고, 국내에서 자연스럽게 통용되는 표현을 사용한다. 불필요한 번역투는 제외한다.
- 코드 식별자, 파일명, 명령어, API·제품 고유명은 원문을 유지한다.
- 기술 용어가 꼭 필요하면 자연스러운 한국어 설명을 먼저 쓰고, 원어는 이해에 도움이 될 때만 괄호로 덧붙인다.
