# VOD Scout v0.4.0 릴리스 기록

상태: **HOLD / BLOCKED — P0 코드 일부는 main(PR #11)에 있으나 PR #13은 draft·open·unmerged(정적 HEAD `e18b73e…`). 제품 버전 정본 0.3.4. 실제 제품 P0 검증은 `4597010…` 1회·종합 BLOCKED(HTTP 403). 새 HEAD 제품 E2E 미재실행. P0 전 게이트 PASS 아님 → 코드·문서 PR 병합 금지 · v0.4.0 버전 범프·패키징·태그·공개 릴리스·배포 실행 계획 미포함**

계획 정본: [V0.4.0 장시간 최적화·이야기 구간 탐색 계획](V0.4.0-PLAN.md)

설계 검토: [V0.4.0 Oracle 설계 검토 기록](V0.4.0-ORACLE-REVIEW.md)

## 목표와 비범위

v0.4.0은 2~8시간 입력에서 계산량과 저장 공간을 통제하면서 전체 영상 흐름과 수분 단위 이야기 후보를 찾는 버전이다. YouTube 입력은 품질을 통과한 원문 자막을 먼저 사용하고, 부족하거나 의심스러운 구간만 로컬 Whisper로 확인한다. Whisper가 필요하면 실제 시험을 통과한 GPU를 우선 사용하고 실패한 청크만 CPU로 처리한다.

완성 쇼츠 자동 제작, 전체 영상 무조건 정밀 음성 인식, 전체 프레임 OCR, Ollama·대형 로컬 언어 모델, 개발자 공용 API 키, Authenticode 인증서 구매·생성은 범위에 포함하지 않는다.

**이번 문서 갱신 범위:** PR #13 정적 HEAD·Oracle 후속 코드·실제 검증 결과와 PASS/HOLD/BLOCKED 경계만 기록한다. **v0.4.0 설치·태그·배포 실행 계획은 포함하지 않는다.** 과거 H8–H11 수치는 역사적 맥락이며 현재 원샷 PASS 근거로 쓰지 않는다.

## 사용자 변경 사항

공개 설치본 버전은 여전히 **0.3.4**다. v0.4.0 사용자 배포 변경은 없다.

- main에 들어간 P0(PR #11): 체크포인트 호환·범위 후보·분석 전 여유 공간·세대 보존·취소 연결 (개발 경로).
- PR #13(미병합, 정적 HEAD `e18b73e…`): YouTube 다운로드 저장 공간 가드·메타데이터 점검·Oracle 후속(포맷 결박·exact size·출력 상한·최소 로그·실패 안내 분리). **실제 전체 내려받기·장시간 분석 검증은 `4597010…`에서 403으로 BLOCKED. 새 HEAD 제품 E2E 미재실행.**

계획된 나머지 사용자 변경(성능 단계, 자막 출처, 이야기 후보, GPU 우선, 선택형 API)은 P1~P6이며 아직 구현·배포하지 않았다.

## 내부 변경 사항과 체크포인트 호환성

### main `cca7a9e…` (PR #11, 병합됨)

- 입력 지문·도구·모델·언어·후보 계산 버전(`rules-v0.4.0-p0`)으로 미디어 체크포인트 호환성 판정. schema 3 이하·불일치 시 미디어만 재계산.
- 비호환 체크포인트 폐기 후 작업 `completed_units`가 앞서 있어도 미디어부터 안전 재시작 (H5F F1 / `d13b864`).
- `.prev` 세대 보존 후 교체.
- 범위 후보 제한·audio/dialogue 근거 필수.
- 분석 시작 전 job 볼륨 free-space (live low-space E2E는 계속 HOLD).
- 미리보기 FFmpeg 취소 플래그. 측정하지 않은 wall-clock ms는 활동에 넣지 않음.
- 단위·정적 PASS. **실제 미디어 비호환 체크포인트 폐기·재계산 시나리오는 미실행 HOLD** (단위와 분리).

### PR #13 `e18b73efcb0ea40be812b7da12572e1207854863` (draft · open · **unmerged**)

- 브랜치: `codex/p0-download-space-guard`
- YouTube acquisition 경로의 저장 공간 가드·메타데이터 점검 사용자 노출 정리.
- **Oracle 후속 구현(정적·단위·독립 재리뷰 PASS, 제품 E2E 미재실행):**
  - 메타 점검이 고른 정확한 `format_id`를 실제 내려받기 `--format`에 결박
  - 공간 계획에 **exact `filesize`만** 허용(대략 크기 거부)
  - 메타 probe stdout **2 MiB** / stderr **256 KiB** 상한 + 자식 정리
  - 최소 구조화 로그
  - 실패 안내 분리
- 최종 독립 재리뷰 **PASS** · 정규(canonical) 6개 명령 **PASS** · 샘플러 **PASS** (`D:\vod-scout-p0-evidence-20260807-023415-pr13-4597010\reports\PR13-final-rereview-e18b73e.md` · SHA-256 `A4B6752524146DD9BDC9033D80DD6CF4D7E37DC5CDCC31CF0F8A2A4EBC340099` · `evidence-index.json` `postOracleCodeFix`).
- **main에 병합하지 않음.** 문서 PR도 병합하지 않음.

공개 버전 정본은 계속 **`0.3.4`**.

## 버그 수정

- (PR #11, 이미 main) 호환되지 않는 미디어 체크포인트 폐기 후 재개 하드 실패 — 미디어 중간 결과만 다시 계산 (H5F F1). 회귀: cargo lib 41 pass / 1 ignored.
- PR #13 범위의 제품 버그 수정으로 **확정된** 항목 없음. 실제 E2E(`4597010…`)는 YouTube **HTTP 403**으로 단말 실패. 원인 귀속(제공처·네트워크 vs 2단계 acquisition 회귀) **HOLD** — 외부 문제로 단정하지 않음. 재시도 없음.

## 보안 수정 (이번 경로에서 확인된 것)

- 자막 확보가 실패해도 로그인·쿠키·나이 제한·봇 확인을 우회하지 않는다.
- PR #13 검증 경로에서 제품 외부 AI 전송 경로를 **새로 추가하지 않았다**(YouTube acquisition HTTPS만 관측). 403은 미디어 바이트 전송 실패이며 원인 귀속은 **HOLD**.
- v0.3.x 제품 경로에 이미 있는 FFmpeg·Whisper·yt-dlp·Deno 환경 초기화·Asset Protocol 범위 등은 기존 보안 통제로 유지한다. **이번 고정 실행 증거에는 사용된 yt-dlp/Deno/FFmpeg/Whisper·DLL·model 경로·SHA 매니페스트 결박이 없어 그 결박 증명은 미실행 HOLD** (코드에 runtime 검증이 없다기보다, 이번 실행과 특정 매니페스트가 묶이지 않음).

## 계획된 보안 요구사항 — 미구현

아래는 P4/P6 등 이후 단계의 **계획된 보안 요구사항**이며, 현재 제품에 구현·배포된 **보안 수정이 아니다.** 구현된 통제처럼 읽히지 않도록 분리한다.

- 사용자 API 키를 Windows 보안 저장소에만 보관하고 설정 파일·작업 폴더·체크포인트·로그·내보내기에 남기지 않기 (P6 선택형 API 도입 시)
- GPU 실행 파일·DLL·모델의 고정 출처·파일 목록·SHA-256 검증 (P4 GPU 묶음 도입 시)
- API·GPU가 없어도 로컬 CPU 규칙 기반 경로로 완료 (P4/P6 설계 원칙 · 선택 기능 미구현 상태)
- 원본 영상·음성·전체 장시간 자막을 외부 API로 보내지 않고 상위 후보 요약만 전송 (P6 설계 원칙 · 외부 AI 경로 미구현)

## 테스트 환경과 결과

### A. PR #13 실제 P0 검증 (2026-08-07 · **현재 정본**)

| 항목 | 값 |
|---|---|
| 제품 E2E 고정 HEAD | `4597010c99bf8432f1fe15ba34269fd63d5daa7c` (클린) · **1회만** |
| 코드 정적 고정 HEAD | `e18b73efcb0ea40be812b7da12572e1207854863` · 제품 YouTube/low-space/장시간 **미재실행** |
| PR 상태 | **#13 draft · open · unmerged** |
| 증거 루트 | `D:\vod-scout-p0-evidence-20260807-023415-pr13-4597010` |
| 보고 | `reports/P0-validation-report-ko.md` · `reports/evidence-index.json` · `reports/ORACLE-final-review-4597010-01a3f6b.md` · `reports/PR13-final-rereview-e18b73e.md` |
| 승인 URL | `https://www.youtube.com/watch?v=JN3BO9GLuFU` |
| 종합 판정 | **BLOCKED** |

#### Oracle 최종 리뷰 (exactly once · 재호출 없음)

| 항목 | 값 |
|---|---|
| 판정 | **BLOCKED** |
| Run | `689ab803-0a7b-4695-ad8e-5a495c3b4991` |
| 보고 SHA-256 | `DB59D334824C07A4F0922C454C476D93CDEADED10980C0910839AFFD00A19815` |
| 요청 모델 | `GPT-5.6 Sol` |
| picker / 선택 | `unavailable` / **검증되지 않음** |

#### 검증용 바이너리 (설치본·릴리스 자산 아님 · `4597010…` 빌드)

| 항목 | 값 |
|---|---|
| 빌드 | `npx tauri build --no-bundle` |
| 버전 메타 | 0.3.4 |
| 크기 | **15,270,400** bytes |
| SHA-256 | **`2914E618F4F99A075C91E8CC888BD2E277ACFF57CEB34ED2F324519AE49B7D98`** |

**공개 설치 EXE·Release 자산·`D:\VOD Scout` 설치 치환 대상이 아니다.**

#### 게이트 표

| 게이트 | 판정 |
|---|---|
| checkout 고정·클린 | **PASS** |
| 독립 코드 재리뷰 @ `e18b73e…` | **PASS** — `PR13-final-rereview-e18b73e.md` · SHA-256 `A4B6752524146DD9BDC9033D80DD6CF4D7E37DC5CDCC31CF0F8A2A4EBC340099` |
| 정규(canonical) 6개 명령 @ `e18b73e…` | **PASS** |
| 샘플러 스모크 | **PASS** (3/3) |
| 실제 low-space 격리 E2E | **HOLD** (`safe low-space E2E environment unavailable`) |
| 신선 create/start 작업 (`4597010…`) | **PASS** (attempt2 1회) |
| 전송 전 메타데이터·저장 공간 점검 | **PASS** (실측) |
| 전체 신선 내려받기 완료 | **BLOCKED** (HTTP 403, 부분 ~10 MB 후 실패) |
| HTTP 403 원인 귀속 | **HOLD** (제공처·네트워크 vs PR #13 2단계 회귀 미분리 · 외부 단정 금지) |
| 중단 없는 장시간 분석 · REVIEW_READY | **BLOCKED** |
| 자원 샘플러 요약 메트릭 | **HOLD** |
| 사용자 앱 데이터 루트 불변 | **HOLD** (WebView/EBWebView 캐시 변동 관측) |
| 사용자 jobs before-after 불변 쌍 | **HOLD** (사전 단독 측정 없음) |
| 설치 폴더 `D:\VOD Scout` 불변 | **HOLD** (453,110,594 bytes / 47 files 전후 동일 · 파일별 SHA 매니페스트 없음) |
| 작업 소유 프로세스 잔존 없음 | **PASS** (403 정상 종료 경로) |
| probe 취소·hang 실제 E2E | **HOLD** (미실행 · 정적 분기만) |
| 실행 시 yt-dlp/Deno/FFmpeg/Whisper·DLL·model 매니페스트 결박 | **HOLD** (이번 증거에 경로·SHA 목록 없음) |
| 실제 체크포인트 마이그레이션 | **HOLD** (단위 PASS / 실제 미디어 미실행) |
| 문서 독립 리뷰 (이전) | 미커밋 working-tree 스냅샷 리뷰 — 고정 HEAD 결박 아님. **이번 최종 커밋 후 exact HEAD 리뷰 필요** |

#### 단일 신규 제품 작업

| 항목 | 값 |
|---|---|
| jobId | `fd8c1cc5-3bfc-4e02-b97a-036ff1009f5e` |
| attempt1 | 하네스 전용 — `devUrl` 로드로 create_job 전 실패. 제품 작업 없음 |
| attempt2 | 유일 제품 작업. 재시도·재개 없음 |
| 메타 | 길이 32,000 s · 스트림 합 7,070,731,050 · 피크 추정 15,555,608,310 bytes (~15.56 GB / ~14.49 GiB) · 제목 【20260220 게임】 파피 플레이타임 챕터 4 Poppy Playtime |
| 부분 파일 | `source.f298.mp4.part` **10,033,007** bytes |
| 실패 | `ERROR: unable to download video data: HTTP Error 403: Forbidden` |
| status | `FAILED` · 단계 `YouTube 다운로드 실패` |
| 벽시계 | ~17.29 s · 디스크 피크/최종 ~10.8 MB (완성 소스 없음) |

### B. 병합·단위 (PR #11 / 역사)

| 항목 | 결과 |
|---|---|
| PR #10 squash-merge | **PASS** · `f5c1614…` |
| PR #11 독립 리뷰·squash-merge | **PASS** · `cca7a9e…` (F1 수정 `d13b864` 포함) |
| H7B 단위·정적 6/6 (main 병합 후) | **PASS** · npm 34 · build · cargo 41/1 ignored · fixture 5 · security 6 · `git diff --check` |

### C. 과거 실제 미디어 H8–H11 (역사적 맥락 · **현재 원샷 PASS 아님**)

main `cca7a9e` 시점에 기록된 결과다. **PR #13 제품 E2E HEAD `4597010…` 또는 정적 HEAD `e18b73e…` 검증을 대체하거나 P0 마감 원샷 PASS로 승격하지 않는다.**

| 검증 | 당시 결과 | 현재 취급 |
|---|---|---|
| H8 범위 [60,360] | overall PASS · 후보 5 · 재다운로드 재현 HOLD | 역사 기록 |
| H9 단위 디스크 가드 | PASS · live low-disk HOLD · acquisition 사전 검사 당시 미구현 | live low-space는 PR #13에서도 HOLD |
| H10 취소·재개 | PASS (CANCELLED 242 ms · 재개 3/8 · 후보 8) | 이번 런 미실행 |
| H11 full (재개 포함) | overall PASS 54/54 · 무중단 single-shot HOLD (하니스 os error 32, 제품 결함 아님) | 이번 런 403으로 **BLOCKED** 미도달 |
| P1~P6·v0.4.0 설치 자산 | HOLD | 동일 · **릴리스 계획 미포함** |

## PR 형식 자체 리뷰

### 변경 파일 (코드 PR #13 · 미병합)

YouTube 다운로드 저장 공간 가드·메타데이터 점검·Oracle 후속 포맷 결박·exact size·출력 상한·로그·실패 안내 (`acquisition` 등). 버전 정본·설치 EXE·workflow·모델 변경 없음. **병합하지 않음.**

### 이 문서 세션 변경 파일

문서만: `HANDOFF.md`, `docs/V0.4.0-PLAN.md`, `docs/V0.4.0-RELEASE.md`, `CHANGELOG.md`, `docs/DEBUGGING.md`. **commit/push/PR 병합은 이 역할 범위 밖** (저자만; 리뷰어 별도). 이전 문서 독립 리뷰는 미커밋 스냅샷 대상이었으므로 과장하지 않으며, **이번 최종 커밋 후 exact HEAD 리뷰가 필요**하다.

### 위험과 회귀 가능성

- YouTube 403으로 신선 full 경로 미검증 — **BLOCKED** · 원인 귀속 **HOLD**
- 새 정적 HEAD에서 제품 E2E 미재실행 — 포맷 결박 등 후속이 실제 전송에 미치는 영향 **미확인 HOLD**
- live low-space E2E 미실행 — 디스크 고갈 거절 경로 일부 **HOLD**
- 설치 폴더 내용 불변 미증명(총량만) — **HOLD**
- WebView 캐시·jobs before-after 증거 부족 — 사용자 데이터 불변 **HOLD**
- probe 취소·hang·runtime 매니페스트 결박·실제 체크포인트 마이그레이션 미실행 — **HOLD**
- 과거 H8–H11을 현재 원샷으로 오인하면 병합 게이트가 열릴 위험 → 문서에서 명시적 분리
- 검증용 EXE 해시를 설치 자산으로 오인할 위험 → 설치·릴리스 자산 아님 명시
- 미구현 보안 요구사항을 보안 수정으로 오인할 위험 → 절 분리

### 발견 사항과 조치

| 발견 | 조치 | 결과 |
|---|---|---|
| Oracle 후속 코드 @ `e18b73e…` 독립 재리뷰·정규 6·샘플러 | 문서 반영 · 보고서 SHA-256 `A4B675…` | **PASS** (병합 조건 아님 · 제품 E2E 미재실행) |
| 메타·공간 점검 후 ~10 MB partial → HTTP 403 | 재시도·재개 없음 · BLOCKED · 귀속 HOLD | **BLOCKED** / 귀속 **HOLD** |
| 설치 폴더 총량 동일만 | PASS 하향 → 내용 불변 **HOLD** | **HOLD** |
| low-space E2E 환경 없음 | 실행 안 함 | **HOLD** |
| 자원 요약 파일 부재 | 피크 재계산만 · HOLD | **HOLD** |
| WebView 캐시 변동·jobs 사전 측정 없음 | 불변 주장 안 함 | **HOLD** |
| 미구현 API 키 저장소·GPU SHA·CPU fallback·외부 AI 전송 정책 | `보안 수정`이 아니라 **계획된 보안 요구사항 — 미구현**으로 표기 | 문서 정정 |
| 검증 EXE 15,270,400 / `2914E618…` | 설치·릴리스 자산 아님 명시 | 참고 해시만 |
| Oracle 최종 리뷰 | exactly once · BLOCKED · 재호출 없음 | 기록 |

미확인 하드웨어·입력·외부 서비스는 PASS로 표시하지 않는다.

## 설치본과 SHA-256

**v0.4.0 설치본을 만들지 않았다.** 버전 정본이 모두 `0.4.0`으로 일치하는 exact merged commit에서 빌드한 뒤 설치 EXE, 핵심 바이너리, GPU 묶음, 모델, updater 서명, 체크섬과 SBOM을 `BUILD-MANIFEST.md`와 함께 기록하는 절차는 **아직 계획에 포함하지 않는다** (P0 전 게이트 PASS 조건 미충족).

공개 설치 자산은 계속 **v0.3.4** (`a341bae…`, Release ID `365895027`)다.

### 검증 전용 바이너리 (설치·릴리스 자산 아님)

| 용도 | 크기 | SHA-256 |
|---|---:|---|
| PR #13 제품 E2E 검증 `vod-scout.exe` (`4597010…`, `tauri build --no-bundle`, 메타 0.3.4) | 15,270,400 | `2914E618F4F99A075C91E8CC888BD2E277ACFF57CEB34ED2F324519AE49B7D98` |

과거 H8–H11 세션에서 쓰인 다른 개발 release 해시가 문서·증거에 남아 있어도 **공개 설치 배포 자산이 아니다.**

## 알려진 문제

- PR #13 실제 검증 종합 **BLOCKED** (YouTube HTTP 403 · full download · long uninterrupted analysis). 403 원인 귀속 **HOLD**.
- 새 정적 HEAD `e18b73e…` 제품 E2E 미재실행 → 제품 P0 **계속 BLOCKED/HOLD**.
- live low-space E2E **HOLD**. 자원 요약·설치 폴더 내용 불변·사용자 앱 데이터/jobs before-after **HOLD**.
- probe 취소·hang 실제 E2E, runtime 매니페스트 결박, 실제 체크포인트 마이그레이션 **미실행 HOLD**.
- 성능 측정 전에는 “8시간 완전 지원”, “GPU 지원”, “이야기 구간 정확 탐지”라고 표시할 수 없다.
- 자막 품질 임계값·이야기 후보 합격선은 여러 실제 기준 영상 확보 전까지 `HOLD`.
- Authenticode 없음 → SmartScreen 가능 · 인증서 구매·생성 안 함.
- **릴리스·병합 조건 미충족:** 모든 P0가 PASS가 아니므로 코드 PR·문서 PR 병합과 v0.4.0 버전·설치·태그·배포 실행 계획을 이 문서에 넣지 않는다.

## 롤백 방법

개발/소스 경로 문제는 검증된 공개 설치본 **v0.3.4**로 되돌린다. 기존 작업 폴더와 `current-job.json`은 삭제하지 않는다. 새 스키마는 읽기 전 백업 또는 마지막 정상 세대를 보존하고, 이전 버전이 읽을 수 없는 중간 결과만 명시적으로 다시 계산한다. PR #13은 미병합이므로 공개 `main` 롤백 대상이 아니다. v0.4.0 공개 빌드 시점의 직전 버전·설치 파일 SHA-256은 그때 고정한다.

## 정확한 다음 작업

1. 새 정적 HEAD `e18b73e…`(또는 후속)에서 **재시도 없는** 신선 전체 내려받기 → 장시간 무중단 분석 **PASS**. 403은 제공처·네트워크 vs 2단계 회귀를 분리한 뒤 귀속 확정.
2. 안전 저용량 볼륨으로 live low-space E2E **PASS**. 보호 경로는 파일별 SHA 매니페스트 전후 비교.
3. 자원 요약 파일·사용자 데이터 before-after 쌍·설치 폴더 내용 불변 증거 **PASS**.
4. probe 취소·hang 실제 E2E, runtime 매니페스트 결박, 실제 체크포인트 마이그레이션 증거.
5. 문서 exact HEAD 독립 리뷰 (미커밋 스냅샷 리뷰를 대체).
6. **1–5 모두 PASS 후에만** PR #13·관련 문서 병합 검토.
7. 그 다음 v0.4.0 버전 정렬·설치·태그·배포 계획 초안 (현재 **미포함**).
8. P1–P7은 P0 완료 게이트 이후에만.
