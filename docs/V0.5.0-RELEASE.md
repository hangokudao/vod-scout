# VOD Scout v0.5.0 릴리스 작업 정본

상태: **G1~G7 구현·자동 검증 PASS · G8 로컬 NSIS·PE·격리 앱 실행 PASS · 자동 한국어 자막 선택·제외 트랙 검증 PASS · GTX 1060 직접 GPU·CPU 음성 인식 PASS · 30초 구성요소 자원 측정 MEASURED_NO_THRESHOLD 완료 · 자동 GPU→CPU 제품 전환·일반 YouTube/reference 전체 흐름·Windows UI·1~8시간/동일 입력 자원 비교·서명/공개 패키지 HOLD 또는 BLOCKED**

계획 정본: [v0.5.0 품질·작업 대기열 개선 계획](V0.5.0-PLAN.md)

현재 공개 버전은 v0.4.0이며, 이 worktree의 소스·로컬 패키지 후보 버전은 v0.5.0이다. v0.5.0 공개 Release·다운로드 URL은 아직 만들지 않는다.

## 2026-08-20 Wave 4 자원·release/NSIS 검증

- 승인된 보존 `probe-motion-30s.mp4`(30.0초·3,427,504 bytes·SHA-256 `c20156488327d68e435120a599970ac03bc716aa55d163b57079f1a2dc5b54fc`)로 직접 GPU/CPU만 측정했다. FFmpeg는 `29.755s/0.094s/22,716,416/20,803,584/4,393,690/960,590`, GPU Whisper는 `2.710s/2.438s/273,334,272/1,148,428,288/4,395,267/48`, CPU Whisper는 `8.110s/14.953s/324,505,600/837,447,680/4,396,535/69`(각각 elapsed/CPU/peak WS/peak private/temp peak/final job)이며 세 wrapper rc는 `0`이다. 세 JSON exitCode는 `null`이다. GPU `peakGpuBytesObserved=0`은 per-process sample 없음이므로 GPU memory는 `UNAVAILABLE`/`HOLD`; `nvidia-smi` 1442 MiB는 whole-GPU snapshot이지 app peak이 아니다. 모든 수치는 `MEASURED_NO_THRESHOLD`다.
- 공식 manifest 핀 재준비와 release runtime manifest schema 6의 51/51 파일·해시 검증은 `PASS`다. Linux Node의 `tar.exe` 경로 실패는 `...\wave4-20260820\logs\prepare-media-tools-linux-failure.log`에 보존했다. lockfile v3 `npm.cmd ci` 뒤 단일 Windows `npm.cmd run tauri:build`는 NSIS 생성까지 성공했지만 signing key 부재로 전체 rc `1`로 종료했으며, NSIS 생성·hash는 `PASS`, signing은 `HOLD`다.
- 현재 PE: `vod-scout.exe` 16,274,944 bytes/SHA-256 `8754dc944d8f685195425bb4d3698c8992225888b2b95e9ed484283b7868cced`, File/ProductVersion `0.5.0`; NSIS `VOD Scout_0.5.0_x64-setup.exe` 595,736,201 bytes/SHA-256 `cd024e2d4523c34f4795c0e3d5bca1f72edca82b89d294033194fa465624ca36`, PE `0.5.0`이다. updater private key 부재로 서명은 `HOLD`다.
- fresh 격리 data-dir 앱은 8초 생존 후 테스트 프로세스를 의도적으로 중단했다(`PASS`, 정상 종료 아님). 1~8시간 입력은 승인된 안전한 증거가 없어 `HOLD`, G7 병렬은 disabled/`HOLD`다. YouTube/플레이어/screenshot/자동 fallback은 재실행하지 않았다.

## 2026-08-20 Wave 3 실제 release-app 검증

- Production-protocol unpacked app의 첫 no-cancel YouTube 실행은 `JKYmw9-xMIo&t=8463s`에서 acquisition까지 도달했으나 transfer HTTP 403으로 실패했다. product command/stderr와 successful pinned yt-dlp+Deno control 비교는 `C:\Users\myhan\AppData\Local\Temp\vod-scout-e0b0c58-20260820\product-yt-dlp-command.txt`에 남겼다. 둘은 pinned tools, `298+251`, Deno runtime, Korean auto-caption flags가 같고 control만 HTTP 206/auto-caption save에 성공하므로 product 403 원인은 미확정 상태로 유지한다.
- GPU release-app retest는 task-local 30초 MP4(3,427,504 bytes, SHA-256 `c20156488327d68e435120a599970ac03bc716aa55d163b57079f1a2dc5b54fc`)에서 checkpoint `device=gpu`, `gpu.status=COMPLETED`, `completedGpuUnits=1`, `whisper.cpp-gpu`와 non-empty Korean SRT를 남겼다. 마지막 player-ready 검사 실패로 screenshot/전체 화면 흐름은 `HOLD`다.
- task-local missing-cuBLAS package fault는 strict runtime-file-list integrity guard에서 `FAILED`되어 GPU dependency failure 뒤 CPU fallback을 검증하지 못했다. 해당 fallback gate는 `HOLD`이며 fallback 성공을 직접 CLI 결과로 대체하지 않는다. Evidence: `C:\Users\myhan\AppData\Local\Temp\vod-scout-e0b0c58-20260820\data-gpu-fault\jobs\d715e4c6-f9da-46a8-afd5-30ae635e69d0\snapshot.json`.
- Intact-resource process-only `CUDA_VISIBLE_DEVICES=-1` 독립 시도는 child-process environment allowlist에 의해 제거되어 GPU가 성공했고, `cpuFallback.status=PENDING`으로 남았다. Evidence: `C:\Users\myhan\AppData\Local\Temp\vod-scout-e0b0c58-20260820\data-gpu-fallback-env\jobs\996ef279-3c47-4415-8feb-2d3de24b4bce\media-checkpoint.json`; 추가 fallback 시도는 하지 않았다.

## 2026-08-19 validation-fixes 검증 결과

- 검증 도구는 음성 중심 후보의 `chatScore: null`을 허용하고, `--require-chat-score`에서만 숫자형 채팅 점수를 요구한다. 취소는 저장된 작업 스냅샷을 최대 10초 확인하며, 실패 시 마지막 스냅샷과 예외·구조화 로그를 남긴다. PowerShell 실행기는 앱 경로·스크린샷·증거 경로와 확인된 자식 프로세스 정리를 지원한다.
- 자동 검증은 프런트 49개, Rust 본체 128 passed·1 ignored, fixture-worker 6개, 관련 Node 스크립트 17개, 보안 6개, 빌드 `PASS`다. 변경 코드의 실제 E2E는 G8 빌드 앱으로 시작 단계와 취소 완료를 확인했다.
- 세 승인 URL은 일반 VOD의 한국어 자동 자막 우선 경로를 검증하는 대표 입력이며, 제작자 자막 부재는 `HOLD`나 릴리스 차단 사유가 아니다. 확인한 범위는 `language=ko`, 자동 생성 한국어 SRT, 파일 무결성·기본 시간 범위이며, 제품 snapshot provenance와 Whisper 대체는 별도 `HOLD`다. 겹침·공백 수치는 품질 `PASS`가 아니며 일정한 시간 오프셋과 내용 품질도 `HOLD`다.
- 자막 증거: `JKYmw9-xMIo` — 133570 bytes, SHA-256 `af6aa5d008bbdd36e60f8c07d556da52686cb52be99b660e8e555783b4f510ef`, 2121 cues, start>=end 0, out-of-range 0, reverse 0, adjacent overlaps 1346, exact duplicate groups 0, max positive gap 47.080 sec; `LVZ6hFhlF2k` — 399263 bytes, SHA-256 `24857fa9aee1fd459e040d5939159ca3c0ea45bb69fa0f7ed7925bf5dfcf1efa`, 5832 cues, start>=end 0, out-of-range 0, reverse 0, adjacent overlaps 5364, exact duplicate groups 0, max positive gap 207.361 sec; `ZJMpYThMksM` — 88141 bytes, SHA-256 `64d876c1ff3813bfc2309d1302af60a12618a55ace924843d7a536a4136c6c55`, 1472 cues, start>=end 0, out-of-range 0, reverse 0, adjacent overlaps 880, exact duplicate groups 0, max positive gap 147.040 sec.
- 전체 앱 흐름은 취소 요청 뒤 4779 ms에 `CANCELLED`를 확인했지만 HTTP 403이 검토 화면 전에 발생했다. 기존 앱 403의 원인은 미확정이며 제품 경로 수정은 검증되지 않았고, screen capture와 전체 후보·검토·삭제 흐름은 후속 검증 `HOLD`다. 최종 증거는 `C:\Users\myhan\AppData\Local\Temp\vod-scout-evidence-full-225324\e2e-failure-2026-08-19T13-53-48-401Z-21572.json`과 같은 basename의 `.log`다. 고정 yt-dlp/Deno control은 HTTP `206`과 한국어 자동 자막 저장에 성공했지만 기존 앱 403은 재현하지 못했다. 새 ordinary no-cancel 제품 E2E는 acquisition 전 로컬 CDP/Tauri IPC 평가에서 실패해 앱 흐름 검증이 `BLOCKED`다. 임의 사용자 미디어는 사용하지 않았다.
- 승인된 G8 GPU 패키지 증거: `whisper-cli.exe` 489984 bytes/SHA-256 `4bf174113843613cbec146e73e6820a767e54b0e1c736f2c6d7ab16aac4c245d`, `ggml-cuda.dll` 562600960 bytes/SHA-256 `24af2cd89090175beffdf77cd25c176d76f09c4018644915f302d2de64d67631`, `cudart32_110.dll` 467456 bytes/SHA-256 `b8bfc244dd0916ddf7b45e39c101f165a0d9f4846616eaf34336a2c374409408`, `cudart64_110.dll` 526848 bytes/SHA-256 `ba5c2fb526c4ee4bb218ceb3fa5e8bfde89ce474f38711fdcce802549bf9fc6f`; `cublas64_11.dll` is missing, so the GPU package gate is `BLOCKED`.
- GPU CPU-backend evidence is `C:\Users\myhan\AppData\Local\Temp\vod-scout-evidence-full-225324\gpu-probe\whisper-gpu.log`; valid `probe.srt` is 47 bytes/SHA-256 `5845cd37d6a0bbae0ce13a136d7652d6b5688938ceaf32a6974a20a57a24a97d`. The probe is not a GPU success and no external GPU binary was added.
- 이번 작업은 설치 파일·G8 패키지·모델을 변경하지 않았다. 따라서 패키지 해시·설치본 검증은 기존 G8 증거를 유지하며, 서명·공개 Release는 계속 `HOLD`다.

## 2026-08-20 Wave 2 GPU 패키지·직접 음성 인식 검증

- whisper.cpp `v1.9.1` 공식 GPU archive에는 `ggml-cuda.dll`이 import하는 `cublas64_11.dll`이 없었고, 이 DLL의 `cublasLt64_11.dll` 의존성도 함께 확인했다. archive/import evidence는 `C:\Users\myhan\AppData\Local\Temp\vod-scout-wave2-gpu-20260820\dependency-inspection.log`다.
- 공식 NVIDIA CUDA cuBLAS redistributable `11.11.3.6` URL `https://developer.download.nvidia.com/compute/cuda/redist/libcublas/windows-x86_64/libcublas-windows-x86_64-11.11.3.6-archive.zip`, archive SHA-256 `67b0934a6359e4ee26fff823c356021589d392c4fd49ca12624f570edc08e2b9`, license `CUDA Toolkit`, EULA `https://docs.nvidia.com/cuda/archive/11.8.0/eula/index.html`을 manifest에 기록했다. private `whisper-gpu` 설치 DLL SHA-256은 `cublas64_11.dll` `8ca516b96b29c2fba2344909a896bc1cd7951f6cd11fe595a8a3929c02cccbed`, `cublasLt64_11.dll` `3d06ca4e4893adb7a153ecd23a540e92817c967312b44646d8c3f91b089196e6`이고 license notice `NVIDIA-CUDA-Toolkit.txt` SHA-256은 `17a280713a9cf1930d0f3a946935ca968d9726a64f1a41c9a589a959a673784f`다.
- GTX 1060 3GB / driver `560.94` / `3072 MiB`에서 보존 `probe.wav` 2.0초·64078 bytes·SHA-256 `7695bcad887367c33cf9f9bce6bf0d98b4fd1547d1b2f9b392c4d426ef7a33c1`을 사용했다. GPU direct CLI와 CPU `--no-gpu` direct CLI가 각각 exit `0` 및 non-empty valid 47-byte SRT(SHA-256 `74e9f3ff2da6c73ad7d9bb45ee7f1a5be4a7a8cb29c1c2a33920af9be4ed882c`)를 생성했다. 로그는 `C:\Users\myhan\AppData\Local\Temp\vod-scout-wave2-gpu-20260820\gpu-direct.log`, `cpu-direct.log`다.
- GPU·CPU direct 결과는 `PASS`; 기존 product pipeline 자동 GPU→CPU 전환과 Windows 화면은 직접 CLI 범위 밖이라 `HOLD`다.

## 목표

시청자 약 5~30명 규모의 치지직 버츄얼 스트리머 방송에서 YouTube 한국어 자막을 안전하게 활용하고, GPU·CPU 음성 인식과 후보 품질을 개선하며, 여러 영상을 안전하게 순서대로 처리한다. 핵심 분석은 외부 AI와 유료 API 없이 완료돼야 한다.

## 포함 범위

1. YouTube 한국어 자동 자막 우선, 없거나 사용할 수 없을 때 제작자 한국어 자막 대체와 원본 시간·품질 검증
2. 실제 시험을 통과한 GPU 우선 실행, CPU 대체 처리와 `자동`·GPU·CPU·속도·정확도·CPU 사용량 제어
3. 기존 음성 인식 품질 안전장치의 재사용 계약과 선택 후보 재음성 인식
4. 분석 단계별 렉·CPU·메모리·디스크·자식 프로세스 측정과 자원 제한
5. 후보 8·20·30개 설정, 내용 품질 경고와 선택 이유 표시
6. 여러 영상의 기본 순차 대기열, 순서 변경, 개별 취소, 수동 재시작
7. 측정 결과가 안전할 때만 제공하는 제한적 병렬 처리

## 비범위

- YouTube 자막 검색 UI와 검색 결과의 원본 시각 이동
- 수분 단위 이야기 후보와 멀리 떨어진 관련 장면 연결
- 채팅 글자 인식·작성자 식별·채팅 영역 자동 탐색
- 사용자 API·외부 AI 후보 재정렬
- 완성 쇼츠 렌더링·자동 게시
- Authenticode 인증서 구매·적용

비범위 항목은 구현 중 필요해 보여도 추가하지 않고 후속 목록에 기록한다.

## 확정된 사용자 환경

- 주요 입력은 치지직 저스트채팅·게임 VOD다.
- 채팅은 익명 화면 오버레이이므로 작성자 ID와 고유 참여자 수를 사용하지 않는다.
- 채팅은 현재 VOD의 고정 채팅 영역 움직임이 내부 평소 분포보다 증가했는지를 보조 신호로만 사용하며 실제 메시지·참여자 증가로 표현하지 않는다.
- 현재 움직임 분석으로 일반 문장과 이모티콘을 구분했다고 주장하지 않는다.
- 채팅이 없거나 적어도 오디오·말하기·화면 근거로 작업이 완료돼야 한다.

## 구현 게이트

| 게이트 | 완료 조건 | 현재 상태 |
|---|---|---|
| G0 문서·기준선 | 계획·아키텍처·UI·테스트·인계가 같은 범위를 가리키고 기준 영상의 사용 권한·보관 범위를 확인 | 문서 계약 `PASS`, 기준 영상 `HOLD` |
| G1 YouTube 자막 | 한국어 자동 자막 우선, 자동 자막이 없거나 사용할 수 없을 때 제작자 자막 대체, 제외 트랙, 원본 시간·품질 검증, Whisper 대체와 메타데이터 저장 | 선택 순서·제외 트랙 자동 테스트 `PASS`, 세 대표 입력은 자동 한국어 경로 `PASS`, 제품 snapshot provenance·Whisper 대체 `HOLD` |
| G2 GPU·CPU 제어 | GPU 근거 게이트, 청크별 CPU 1회 대체, 모드·프로필·상태 영속 | 직접 GPU·CPU·자동 테스트 `PASS`, 제품 자동 전환·화면 `HOLD` |
| G3 음성 인식 안전장치 | 불확실 결과 보호, 선택 후보 재음성 인식과 실행 ID | 구현·자동 테스트 `PASS`, 실제 흐름 `HOLD` |
| G4 자원·반응성 | 단계별 기록, 경고·강제 중단 분리, 체크포인트·실패 이유 보존 | 구현·자동 테스트 `PASS`, 30초 구성요소 측정 `MEASURED_NO_THRESHOLD`, GPU per-process memory `UNAVAILABLE/HOLD`, 1~8시간·동일 입력 비교 `HOLD` |
| G5 후보 수·품질 | 8·20·30개 설정·복원, pool/evidence 동기화, 후보 개정·판정 보존 | 구현·자동 테스트 `PASS`, 실제 품질·사람 판정 `HOLD` |
| G6 순차 대기열 | 독립 작업, `INTERRUPTED` 복구, 단일 실행권, 삭제 순서·실패 격리 | 구현·자동 테스트 `PASS`, 실제 3개 영상 흐름 `HOLD` |
| G7 제한적 병렬 | 기본 순차, 측정 전 fail-closed, 순차 전환 영속 | 구현·자동 테스트 `PASS`, 병렬 옵션 사용 불가·실제 측정 `HOLD` |
| G8 통합·패키지 | 버전 정렬·자동 테스트·로컬 패키지·해시·진입점 확인 | 소스 통합·자동 테스트·NSIS/PE/hash·격리 앱 실행·GPU/CPU direct `PASS`, 자동 전환·서명/공개 자산 `HOLD` |

G1~G7은 [v0.5.0 계획](V0.5.0-PLAN.md)의 순서대로 앞 단계의 데이터 계약과 검증이 통과한 뒤 진행한다. 병렬 구현 때문에 공통 체크포인트·상태 파일을 여러 작업자가 동시에 수정하지 않는다.

## 종료가 보장되는 처리 규칙

- 실패·취소 작업은 자동 재시작하지 않는다. 사용자가 요청할 때만 재시작한다.
- 같은 청크의 GPU 실패 뒤 CPU 자동 전환은 한 번만 수행한다.
- GPU 실패와 CPU 대체 처리 상태를 청크별로 저장한다. GPU 실패가 기록된 청크는 앱 재실행 뒤 GPU로 돌아가지 않으며, CPU 처리도 실패하면 해당 작업을 `FAILED`로 끝내고 다음 대기 작업을 시작한다.
- 품질 경고만으로 후보를 자동 재분석하지 않는다. 사용자 행동 한 번당 실행 ID 하나만 만들며 완료·실패가 다른 재분석을 자동 호출하지 않는다.
- YouTube는 일반 VOD에서 한국어 자동 자막을 먼저 사용하고, 자동 자막이 없거나 사용할 수 없을 때만 제작자 한국어 자막을 사용한다. 자동 번역·다른 언어·`live_chat`은 제외한다. 원본 영상 시간 기준을 유지하며 각 시작 시각이 끝 시각보다 앞서고 두 시각이 원본 영상 길이 범위 안에 있는지, 겹침·중복·긴 공백·일정한 시간 오프셋을 확인한다. 두 자막이 없거나 검증할 수 없는 자막·구간은 로컬 Whisper로 처리한다.
- 자막 작업 데이터에는 원본 자막 파일, 제작자·자동 출처, 언어, 트랙 식별자, 파일 SHA-256을 남긴다. 실제 기준 영상 전에는 시간 허용 오차나 시간 오차 합격 임계값을 정하지 않는다.
- 음성 인식 모드는 `자동(GPU 우선)`·GPU·CPU, 속도·정확도 단계와 CPU 사용량 제어를 제공하며, GPU는 실제 시험 성공 뒤에만 사용한다.
- 병렬 처리의 자원 조건을 충족하지 못하면 순차 처리로 한 번 전환해 상태를 저장하고, 앱 재실행 뒤에도 다시 자동으로 병렬화하지 않는다.
- 앱 재실행 시 실행 중이던 작업은 `INTERRUPTED`로 멈추며 사용자 재개·취소 전에는 자동 재개하거나 다음 작업을 시작하지 않는다.
- 강제 자원 상한 초과 시 현재 작업 소유 자식만 종료하고 체크포인트와 이유를 보존한 뒤 `FAILED`로 끝내며 설정을 바꿔 자동 재시작하지 않는다.

## 호환성과 데이터 보존

- v0.4.0 작업을 열기만 해서는 기존 후보를 다시 계산하거나 덮어쓰지 않는다.
- 체크포인트 구조가 바뀌면 스키마 버전과 호환 판정을 먼저 추가한다.
- 후보 수 변경·수동 재분석 결과는 새 후보 개정으로 저장한다. 동일한 안정적 후보 ID에만 판정을 이어받고 ID가 달라진 후보는 미판정으로 시작한다.
- 이전 후보 개정, 사용자 판정, 원본 타임코드와 스키마 변경 전 v0.4.0 정상본을 보존한다.
- 대기열의 각 영상은 독립된 작업 ID와 작업 폴더를 사용한다.
- 시작 전 대기 작업과 종료 상태 작업만 즉시 삭제한다. 실행 중 삭제는 취소, 해당 작업 자식 프로세스 0개 확인, 종료 상태 저장 뒤 정규화한 `jobs/{job-id}` 하위 폴더를 삭제하고, 성공한 뒤에만 대기열 참조를 제거한다.
- 개별·전체 삭제가 실패하면 참조와 `삭제 실패` 상태를 유지하며 다른 작업이나 대기열 순서를 변경하지 않는다.

## 보안과 외부 전송

- 새 외부 AI·유료 API·API 키 저장 경로를 추가하지 않는다.
- 원본 영상·음성·음성 인식 결과는 외부로 보내지 않는다.
- GPU 실행 파일·DLL·모델을 추가할 경우 출처·버전·SHA-256·라이선스를 고정하고 설치 경로에서 다시 검증한다.
- 로그에는 인증 정보, 개인 절대 경로, 원시 외부 도구 전체 출력을 남기지 않는다.

## 필수 검증

기본 자동 검증:

```powershell
npm.cmd test
npm.cmd run build
cargo test --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/fixture-worker/Cargo.toml
```

추가로 [테스트와 완료 기준](TEST-PLAN.md)의 v0.5.0 항목을 실행한다. 실제 영상·GPU·설치본 검증을 실행하지 못하면 결과를 `HOLD`로 남긴다.

### G8 통합 실행 결과 (2026-08-18, local worktree)

- `npm ci`: PASS. `npm test`: 49 passed (3 files).
- `npm run build`: PASS (TypeScript + Vite, 1,793 modules).
- `cargo.exe test --manifest-path src-tauri/Cargo.toml`: 128 passed, 1 ignored. `cargo.exe test --manifest-path src-tauri/fixture-worker/Cargo.toml`: 6 passed.
- `npm run test:security`: 6 passed. `node --test scripts/archive-safety.test.mjs scripts/prepare-media-tools.test.mjs scripts/sample-disk-usage.test.mjs`: 11 passed.
- `npm run tauri:build` (Windows PowerShell): NSIS 생성·핵심 EXE·격리 앱 실행 `PASS`; 명령은 updater 개인키 부재로 서명 단계에서 `HOLD` 종료했다. FFmpeg 새 고정 자산 다운로드·SHA-256 검증과 media-tools 준비는 통과했다.
- 로컬 `vod-scout.exe`: 16,270,848 bytes, SHA-256 `d29cbf3f2d55e993ef896ecddcc202b6586e0a335f8cc6692fc51dcca1ac2d2f`, PE ProductVersion/FileVersion `0.5.0`.
- 로컬 `VOD Scout_0.5.0_x64-setup.exe`: 595,736,201 bytes, SHA-256 `cd024e2d4523c34f4795c0e3d5bca1f72edca82b89d294033194fa465624ca36`, PE ProductVersion/FileVersion `0.5.0`.
- fresh `VOD_SCOUT_E2E_DATA_DIR`에서 빌드 앱이 8초 생존했고 프로세스 종료 후 격리 폴더에 `instance.lock`·`queue.json` 2개만 생성됐다. 기존 설치·사용자 데이터는 건드리지 않았다.
- 실제 YouTube/reference 전체 흐름, 자동 GPU→CPU 제품 전환, Windows UI, 1~8시간·동일 입력 자원 비교, G7 병렬 측정: HOLD 또는 BLOCKED. GTX 1060 직접 GPU·CPU CLI는 PASS이며 30초 구성요소 측정은 `MEASURED_NO_THRESHOLD`다. 기존 설치 앱·사용자 데이터는 건드리지 않았다.

## PR 형식 리뷰 게이트

- 입력 검증과 작업 ID·삭제 경계
- v0.4.0 체크포인트·사용자 판정 호환성
- 취소와 앱 종료 뒤 자식 프로세스 정리
- 단일 앱 실행권과 `INTERRUPTED` 작업의 수동 복구
- 대기열 실패가 다음 작업을 막지 않는지
- 자동 재시작·GPU 재시도·병렬 전환이 반복되지 않는지
- 외부 바이너리 출처·SHA-256·라이선스
- 개인정보·외부 전송·API 키 경로가 새로 생기지 않았는지
- 장시간 메모리·임시 파일·동시 프로세스 상한

미확인 항목은 `PASS`로 바꾸지 않는다.

### G8 로컬 PR 형식 자체 리뷰 결과

- **PASS** — 입력·URL allowlist, UUID 작업 ID와 삭제 범위, 체크포인트/후보 pool 호환, 자식 프로세스 종료, 외부 전송/API 키 비추가, G5/G6 자동 회귀와 G7 fail-closed를 코드·테스트에서 확인했다.
- **PASS** — 외부 바이너리 manifest·라이선스 경로와 archive/media-tool 안전성 테스트를 확인했다.
- **HOLD** — 실제 1~8시간 자원·임시 파일·동시 실행 측정, 실제 GPU/reference-video/Windows UI는 실행하지 않았다.
- **PASS** — 공식 FFmpeg asset `autobuild-2026-08-17-13-05`의 URL·archive·SHA-256을 고정하고 NSIS installer·PE/hash·fresh 격리 앱 entrypoint를 확인했다.
- **HOLD** — updater 서명 키가 없어 `.sig`와 공개 Release 자산은 생성·검증하지 않았다.
- **BLOCKED** — 없음. 외부 Release·remote·기존 설치·사용자 데이터 변경은 시도하지 않았다.

## 알려진 한계와 HOLD

- 기준 영상의 사용 권한·보관 범위와 실제 입력은 아직 확정하지 않았다.
- 실제 GPU 지원 범위는 시험 전까지 확정하지 않는다. GPU·CPU 사용자 설정과 화면 흐름도 아직 확인하지 않았다.
- 실제 YouTube 자막 기준 영상·시간 오프셋·품질 검증은 아직 실행하지 않았다.
- G3 구현·자동 테스트는 PASS이나 선택 후보 재음성 인식의 실제 사용자 흐름은 아직 확인하지 않았다.
- Authenticode 부재와 SmartScreen 경고는 v0.5.0 비범위다.
- YouTube 봇 확인은 로그인 자동화나 CAPTCHA 우회로 해결하지 않는다.
- 실제 기준 영상의 단계별 자원·장시간·병렬 비교와 v0.4.0 exact HEAD의 순간 최대 임시 파일 재측정은 HOLD다.
- 로컬 NSIS installer·PE 버전·크기·SHA-256과 fresh 격리 앱 실행은 `PASS`다. updater 서명 키 부재로 `.sig`와 공개 자산은 `HOLD`다.

## 롤백

- 기능 PR 병합 전 회귀가 확인되면 변경을 `main`에 반영하지 않고 공개 v0.4.0 정본을 유지한다. 작업 브랜치 삭제는 별도 승인 없이는 수행하지 않는다.
- 병합 후 회귀가 확인되면 v0.5.0 태그·자산을 덮어쓰지 않고 수정 버전을 준비한다.
- 롤백 과정에서도 기존 작업 폴더와 설치 폴더를 자동 삭제하지 않는다. 스키마 변경 전 보존한 v0.4.0 정상본과 이전 후보 개정을 유지해 공개 v0.4.0에서 읽던 원본 작업을 복구할 수 있게 한다.

## 완료 시 갱신할 기록

- `package.json`, `src-tauri/Cargo.toml`, `src-tauri/tauri.conf.json`의 버전
- `CHANGELOG.md`의 실제 `Added`, `Changed`, `Fixed`, `Security`, `Known issues`
- `docs/DEBUGGING.md`의 재현 조건·원인·수정·회귀 테스트
- `HANDOFF.md`의 마지막 PASS와 다음 정확한 작업
- `BUILD-MANIFEST.md`의 설치 파일·핵심 바이너리 크기와 SHA-256
- README 설치 링크, 설치 파일명, updater 안내와 공개 Release 자산

실제 결과가 없는 항목에는 예정값이나 추정값을 쓰지 않는다.

현재 G1~G7 구현과 자동 테스트, 소스 버전 정렬, 로컬 NSIS·PE/hash·격리 앱 실행, GTX 1060 직접 GPU·CPU CLI 음성 인식은 PASS다. 30초 구성요소 자원 측정은 `MEASURED_NO_THRESHOLD`로 완료했다. 실제 YouTube/reference 전체 흐름, 자동 GPU→CPU 제품 전환, Windows UI, 1~8시간·동일 입력 자원 비교와 G7 병렬 측정은 실행하지 않았고 HOLD 또는 BLOCKED이며 G7 병렬 옵션은 disabled다. updater 서명 키 부재로 `.sig`·공개 Release는 HOLD다.
