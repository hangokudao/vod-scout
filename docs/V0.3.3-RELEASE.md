# VOD Scout v0.3.3 릴리스 기록

상태: **HOLD** (구현·실제 미디어·8시간대 빠른 분석은 PASS, PR 병합·설치본·서명·업데이트 전환은 승인 전)

## 목표와 비범위

v0.3.3은 후보 비교와 원본 앞뒤 맥락 검토 흐름을 개선한다. GPU 음성 인식, 성능 단계, 8시간 최적화, 이야기 구간 탐색, 외부 API·LLM, 검색어 기능은 포함하지 않는다.

## 사용자 변경 사항

- 후보를 종합 점수, 원본 시간, 오디오 반응, 대화 밀도, 채팅 움직임, 판정 상태로 정렬한다.
- 정렬 순서를 바꿔도 후보 ID를 기준으로 선택과 판정을 유지한다. 동점은 원본 시간과 후보 ID 순으로 고정한다.
- Windows 시스템 설정을 따르는 밝은 화면·어두운 화면과 수동 선택을 제공하고 설정을 저장한다.
- 후보 앞뒤 영상 구간, 원본 타임코드, 음성 인식 문장을 표시하고 후보 시작·맥락 시작·맥락 끝으로 이동한다.
- 업데이트 확인 중, 최신 상태, 새 버전, 분석 종료 후 설치 대기, 연결 실패를 구분한다.

## 내부 변경 사항과 체크포인트 호환성

- Candidate ID는 시작·끝 원본 초를 함께 사용해 안정적이며 같은 시작·다른 끝 구간을 구분한다.
- 맥락 구간은 원본 시작·끝으로 제한되고 기본 앞뒤 여유 시간은 15초다. 저장된 후보에는 맥락 범위와 타임코드 문장이 포함된다.
- 맥락 프록시 캐시 키는 작업 ID, 후보 ID, 원본 fingerprint, 맥락 시작·끝, 프록시 종류를 모두 포함한다.
- 이전 후보 snapshot은 새 맥락 필드가 없어도 기본값으로 읽도록 유지한다.

## 버그 수정

- 같은 시작 초를 가진 후보가 선택 상태를 공유할 수 있던 위험을 시작·끝 구간 ID로 수정했다. 동일 입력 재생성과 같은 시작·다른 끝 회귀 테스트를 추가했다.
- 실제 미디어에서 후보·맥락 미리보기를 만들 때 임시 이름이 `.mp4.tmp`가 되어 FFmpeg가 출력 형식을 고르지 못하던 문제를 수정했다. 최종 파일의 MP4 확장자를 유지하는 `.tmp.mp4`를 사용하고 회귀 테스트를 추가했다.

## 보안 수정

새 외부 전송이나 API 키 경로는 추가하지 않았다. 프록시는 기존 작업 폴더 경계와 원본 fingerprint 검증을 유지한다. 측정용 검토 재생은 설정에 이미 허용된 `$APPLOCALDATA/e2e-*` 격리 경로에서 수행했으며 Asset Protocol 범위를 넓히지 않았다.

## 테스트 환경과 결과

| 검사 | 결과 |
|---|---|
| `npm.cmd test` | PASS, Windows에서 2개 파일·32개 테스트 |
| `npm.cmd run build` | PASS, TypeScript·Vite, 1,793 modules |
| `cargo test --manifest-path src-tauri/Cargo.toml` | PASS, 27개 통과·0개 실패·실제 미디어 전용 1개 무시 |
| `cargo test --manifest-path src-tauri/fixture-worker/Cargo.toml` | PASS, 5개 통과·0개 실패 |
| `npm.cmd run test:security` | PASS, 정상 archive 1개·공격 경로 5개, 총 6개 |
| 승인된 실제 영상 검토 흐름 | PASS, YouTube `JN3BO9GLuFU`, 실제 `08:53:19.981`, 720p, 빠른 분석 `11/11`, `17/17`, 후보 8개, `REVIEW_READY` |
| 후보·맥락 미리보기 | PASS, 실제 입력으로 H.264/AAC 맥락 75초·후보 49초 생성, 플레이어 준비 상태 확인 |
| 8시간대 처리 시간 | PASS, 첫 시작부터 검토 준비 `36분 01.231초`, 중단 사이 대기 제외 누적 활성 `34분 28.605초`; 측정 도구 중단 2회 뒤 체크포인트 재개 |
| 8시간대 자원 | PASS, 최대 working set `934,158,336 bytes`, private bytes `1,123,246,080 bytes`, GPU 보드 메모리 최대 `2,012 MiB`·기준선 대비 `+330 MiB`, 분석 WAV 최대 `19,200,078 bytes`, 최종 작업 데이터 `7,097,358,568 bytes` |
| 내려받기 순간 최대 임시 용량 | HOLD, 병합 중 열린 출력 파일 길이를 표본 수집기가 읽지 못함; 닫힌 입력 파일 최소 확인값 `7,070,731,050 bytes` |
| 서명된 v0.3.2 → v0.3.3 설치·재실행 | HOLD, v0.3.3 설치본 미생성 |

## PR 형식 자체 리뷰

### 변경 파일

- `src/App.tsx`, `src/App.css`, `src/App.test.tsx`, `src/api.ts`, `src/types.ts`
- `src-tauri/src/domain.rs`, `src-tauri/src/media.rs`, `src-tauri/src/lib.rs`, `src-tauri/fixture-worker/src/main.rs`
- `.github/workflows/installer-smoke.yml`, `.github/workflows/release.yml`, `scripts/generate-release-assets.mjs`
- 버전 정본과 v0.3.3 릴리스·updater·인계 문서

### 위험과 회귀 가능성

- 맥락 프록시 생성은 실제 미디어 파일과 Windows 도구 번들이 필요한 경로이며, 이번 실제 입력에서 발견한 FFmpeg 임시 확장자 오류를 수정하고 재생까지 확인했다.
- 빠른 분석은 전체 32,000초 중 6,400초를 시간대별로 골라 처리한다. 이번 결과는 전체 정밀 분석 성능이나 모든 하이라이트 탐지를 보장하지 않는다.
- 측정 도구 오류로 앱을 두 번 다시 시작했으므로 처리 시간은 체크포인트 재개를 포함한 누적 활성 시간이다. 중단 없는 단일 실행 시간으로 주장하지 않는다.
- 업데이트·설치 전환은 v0.3.3 설치본과 공개 업데이트 경로가 준비된 뒤 확인해야 한다.

### 발견 사항과 조치

- PASS: 입력 후보의 점수·판정을 바꾸지 않는 정렬과 결정적 동점 처리를 확인했다.
- PASS: 이전 snapshot의 누락 맥락 필드 기본값과 후보 ID 회귀 테스트를 확인했다.
- PASS: 승인된 8시간 53분 입력에서 내려받기, 분석, 두 차례 체크포인트 재개, 후보 생성, 맥락·후보 MP4 생성과 플레이어 재생을 확인했다. 종료 뒤 관련 자식 프로세스는 0개였다.
- PASS: Tauri no-bundle 시험 실행 파일은 버전 `0.3.3`, 크기 `15,187,456 bytes`, SHA-256 `8F6F052A61773F0963C4460B6AB1922FD4B6AAC0766672A25104BF86DDBDED7E`다. 병합 뒤 exact merged commit에서 다시 만들어야 한다.
- HOLD: 설치 EXE·updater 서명·SBOM·공개 업데이트 전환은 승인 전이라 생성하거나 실행하지 않았다.
- HOLD: 내려받기 병합 중 순간 최대 임시 용량은 열린 출력 파일 길이 측정 한계로 확정하지 못했다.

## 설치본과 SHA-256

v0.3.3 설치본은 승인 전이라 아직 만들지 않았다. 설치 파일명은 `VOD.Scout_0.3.3_x64-setup.exe`로 고정하고 updater 표시 문구는 [`V0.3.3-UPDATER-NOTES.md`](./V0.3.3-UPDATER-NOTES.md)를 사용한다. GitHub 저장소에는 기존 `TAURI_SIGNING_PRIVATE_KEY`, `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` secret 이름이 존재하지만 로컬 환경에는 키가 없으므로 서명 PASS로 보지 않는다. 현재 `SBOM.spdx.json`도 v0.3.2이므로 v0.3.3 근거가 아니다. 설치 EXE·updater 자산의 실제 크기·SHA-256·서명·SBOM은 PR 병합 뒤 exact merged commit 설치 게이트에서 `BUILD-MANIFEST.md`와 `SHA256SUMS.txt`에 기록한다.

## 알려진 문제

- Windows 코드 서명 인증서가 없으면 SmartScreen 경고가 표시될 수 있다.
- 현재 PC의 CurrentUser·LocalMachine 인증서 저장소에서 Authenticode 코드 서명 인증서를 찾지 못했고 `signtool.exe`도 확인되지 않았다. 구매하거나 대체 인증서를 만들지 않으며 `HOLD`다.
- v0.3.3 설치본, updater 서명, v0.3.2에서의 실제 업데이트·재실행·기존 작업 보존은 검증 전까지 `HOLD`다.
- 8시간대 빠른 분석은 체크포인트 재개 포함 결과이며 내려받기 병합 순간의 정확한 최대 임시 용량은 `HOLD`다.

## 롤백 방법

PR 병합 전에는 작업 브랜치와 PR #5를 유지하고 `main`에는 쓰지 않는다. 릴리스 후 문제가 발견되면 승인된 이전 v0.3.2 설치본과 체크섬으로 되돌리고 v0.3.3 설치본은 별도 릴리스 자산으로 취급한다. v0.3.2 작업 데이터와 체크포인트는 삭제하지 않는다.
